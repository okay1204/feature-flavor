import { RecipeList } from "@/components/RecipeList";

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center min-w-screen">
      <RecipeList />
    </main>
  );
}
